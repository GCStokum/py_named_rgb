py_named_rgb
============

A collection of rgb valued colors that can be called name.

py_named_rgb.py is intended to give easy access to a (140+) pallet of colors --as defined by the W3-- in both their named ("RED" or "ALICE BLUE") identifier, as well as their RGB 0-255 (###,###,###) format.
being able to be called via rgb.DarkSlateGray

for more information on this pallet, please see http://www.w3schools.com/html/html_colorvalues.asp

TODO: add a demo run alone mode for the .py that displayes an on screen palette example.
    // Probably implemented via a call to pygame.
